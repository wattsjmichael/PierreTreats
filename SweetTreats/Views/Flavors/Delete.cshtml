@{
  Layout = "_Layout";
}

@model SweetTreats.Models.Flavor

<h2>Are you sure you want to delete this?</h2>

@Html.DisplayNameFor(model => model.FlavorName): @Html.DisplayFor(model => model.FlavorName)
@using (Html.BeginForm())
{
    <input type="submit" value="Delete" />
}
@Html.ActionLink("Back to Flavors", "Index", null, null, new { @class = "btn btn-outline-dark btn-block"})</p>